import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'CryptoDash - Advanced Cryptocurrency Dashboard',
  description: 'A modern, feature-rich cryptocurrency dashboard with real-time data, advanced analytics, and portfolio management.',
  keywords: ['cryptocurrency', 'bitcoin', 'ethereum', 'trading', 'portfolio', 'dashboard'],
  authors: [{ name: 'CryptoDash Team' }],
}

export const viewport = {
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
} 